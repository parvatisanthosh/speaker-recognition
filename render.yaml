services:
  - type: web
    name: dhwani-speaker-recognition
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y libsndfile1 ffmpeg
      pip install -r requirements.txt
    startCommand: gunicorn speaker_recognition_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PYANNOTE_AUTH_TOKEN
        sync: false